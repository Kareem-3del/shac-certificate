<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>User List - Admin Panel</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <style>
        :root {
            --color-primary: 59 130 246;
            --color-secondary: 99 102 241;
            --color-accent: 34 197 94;
            --color-background: 249 250 251;
            --color-surface: 255 255 255;
        }
        
        * {
            font-family: "Inter", sans-serif;
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, rgb(var(--color-primary)) 0%, rgb(var(--color-secondary)) 100%);
        }
        
        .card-hover {
            transition: all 0.3s ease;
        }
        .card-hover:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        
        .animate-fade-in {
            animation: fadeIn 0.6s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .stat-card {
            background: linear-gradient(135deg, rgb(var(--color-surface)) 0%, rgb(248 250 252) 100%);
            border: 1px solid rgb(226 232 240);
        }
        
        .nav-item {
            transition: all 0.2s ease;
        }
        .nav-item:hover {
            background-color: rgb(241 245 249);
            border-left: 4px solid rgb(var(--color-primary));
        }
        .nav-item.active {
            background-color: rgb(239 246 255);
            border-left: 4px solid rgb(37 99 235);
            color: rgb(29 78 216);
        }
    </style>
</head>
<body class="min-h-screen" style="background-color: rgb(var(--color-background))">
    <!-- Top Navigation -->
    <nav class="shadow-sm border-b fixed w-full top-0 z-50" style="background-color: rgb(var(--color-surface)); border-color: rgb(229 231 235)">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <img class="h-8 w-auto" src="https://shacpr.org.sa/images/sha.png" alt="SHA Logo">
                    <div class="ml-4">
                        <h1 class="text-xl font-semibold text-gray-900">User Management</h1>
                    </div>
                </div>
                
                <div class="flex items-center space-x-4">
                    <div class="flex items-center space-x-2">
                        <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
                        <span class="text-sm text-gray-500">Live</span>
                    </div>
                    <div class="text-sm text-gray-600">
                        <i class="fas fa-calendar mr-1"></i>
                        <span id="currentDate"></span>
                    </div>
                    <a href="/" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                        <i class="fas fa-home mr-1"></i>Main Site
                    </a>
                    <a href="/auth/logout" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                        <i class="fas fa-sign-out-alt mr-1"></i>Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="flex pt-16">
        <!-- Sidebar Navigation -->
        <div class="w-64 shadow-sm border-r min-h-screen" style="background-color: rgb(var(--color-surface)); border-color: rgb(229 231 235)">
            <div class="p-6">
                <nav class="space-y-2">
                    <a href="/dashboard" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-tachometer-alt mr-3 text-lg"></i>
                        Dashboard Overview
                    </a>
                    <a href="/dashboard/certificates" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-certificate mr-3 text-lg"></i>
                        Certificate Management
                    </a>
                    <a href="/dashboard/users" class="nav-item active flex items-center px-4 py-3 text-sm font-medium rounded-lg">
                        <i class="fas fa-users mr-3 text-lg"></i>
                        User Management
                    </a>
                    <a href="/dashboard/analytics" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-chart-line mr-3 text-lg"></i>
                        Analytics & Reports
                    </a>
                    <a href="/dashboard/subscriptions" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-crown mr-3 text-lg"></i>
                        Subscriptions
                    </a>
                    <a href="/dashboard/settings" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-cog mr-3 text-lg"></i>
                        System Settings
                    </a>
                    <a href="/dashboard/email" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-envelope mr-3 text-lg"></i>
                        Bulk Email
                    </a>
                    <a href="/dashboard/transactions" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-credit-card mr-3 text-lg"></i>
                        Transactions
                    </a>
                    <a href="/dashboard/audit" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-clipboard-list mr-3 text-lg"></i>
                        Audit Logs
                    </a>
                    <a href="/dashboard/advertising" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-bullhorn mr-3 text-lg"></i>
                        Ad Campaign ROI
                    </a>
                </nav>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 p-8">
            <!-- Dashboard Header -->
            <div class="mb-8 animate-fade-in">
                <h2 class="text-3xl font-bold text-gray-900 mb-2">User Management</h2>
                <p class="text-gray-600">View and manage all user accounts</p>
            </div>

            <!-- Stats Cards -->
            <div class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-8">
              <div class="bg-white rounded-xl shadow-sm p-6 card-hover transition-all duration-300">
                <div class="flex items-center">
                  <div class="flex-shrink-0">
                    <div class="w-8 h-8 bg-blue-100 rounded-lg flex items-center justify-center">
                      <svg class="w-5 h-5 text-blue-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0z"></path>
                      </svg>
                    </div>
                  </div>
                  <div class="ml-4">
                    <p class="text-sm font-medium" style="color: rgb(107 114 128)">Total Users</p>
                    <p class="text-2xl font-bold text-gray-900"><%= userStats.total %></p>
                  </div>
                </div>
              </div>

              <div class="bg-white rounded-xl shadow-sm p-6 card-hover transition-all duration-300">
                <div class="flex items-center">
                  <div class="flex-shrink-0">
                    <div class="w-8 h-8 bg-green-100 rounded-lg flex items-center justify-center">
                      <svg class="w-5 h-5 text-green-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                      </svg>
                    </div>
                  </div>
                  <div class="ml-4">
                    <p class="text-sm font-medium" style="color: rgb(107 114 128)">New Today</p>
                    <p class="text-2xl font-bold text-gray-900"><%= userStats.growth %></p>
                  </div>
                </div>
              </div>

              <div class="bg-white rounded-xl shadow-sm p-6 card-hover transition-all duration-300">
                <div class="flex items-center">
                  <div class="flex-shrink-0">
                    <div class="w-8 h-8 bg-purple-100 rounded-lg flex items-center justify-center">
                      <svg class="w-5 h-5 text-purple-600" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2-2V7a2 2 0 012-2h2a2 2 0 002 2v2a2 2 0 002 2h2a2 2 0 002-2V7a2 2 0 012-2h2a2 2 0 012 2v2a2 2 0 01-2 2h-2a2 2 0 00-2 2v6a2 2 0 01-2 2H9z"></path>
                      </svg>
                    </div>
                  </div>
                  <div class="ml-4">
                    <p class="text-sm font-medium" style="color: rgb(107 114 128)">This Month</p>
                    <p class="text-2xl font-bold text-gray-900"><%= userStats.newInPeriod %></p>
                  </div>
                </div>
              </div>
            </div>

            <!-- Users Table -->
            <div class="bg-white rounded-xl shadow-sm card-hover transition-all duration-300">
              <div class="p-6 border-b" style="border-color: rgb(229 231 235)">
                <div class="flex justify-between items-center">
                  <div>
                    <h3 class="text-lg font-semibold text-gray-900">User Accounts</h3>
                    <p class="text-sm text-gray-600 mt-1">Manage all user accounts in your system</p>
                  </div>
                  <div class="flex space-x-2">
                    <select class="px-3 py-1 text-sm border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" style="border-color: rgb(209 213 219)">
                      <option value="all">All Roles</option>
                      <option value="admin">Admin</option>
                      <option value="moderator">Moderator</option>
                      <option value="user">User</option>
                    </select>
                    <input type="text" placeholder="Search users..." class="px-3 py-1 text-sm border rounded-lg focus:ring-2 focus:ring-blue-500 focus:border-transparent" style="border-color: rgb(209 213 219)">
                  </div>
                </div>
              </div>
              
              <div class="overflow-x-auto">
                <table class="w-full">
                  <thead class="bg-gray-50">
                    <tr>
                      <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider" style="color: rgb(107 114 128)">User</th>
                      <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider" style="color: rgb(107 114 128)">Role</th>
                      <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider" style="color: rgb(107 114 128)">Points</th>
                      <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider" style="color: rgb(107 114 128)">Created</th>
                      <th class="px-6 py-3 text-left text-xs font-medium uppercase tracking-wider" style="color: rgb(107 114 128)">Actions</th>
                    </tr>
                  </thead>
                  <tbody class="bg-white divide-y" style="divide-color: rgb(229 231 235)">
                    <% users.forEach(function(targetUser) { %>
                    <tr class="hover:bg-gray-50 transition-colors">
                      <td class="px-6 py-4 whitespace-nowrap">
                        <div class="flex items-center">
                          <div class="w-10 h-10 bg-blue-100 rounded-full flex items-center justify-center">
                            <span class="text-blue-600 font-medium text-sm"><%= targetUser.username.charAt(0).toUpperCase() %></span>
                          </div>
                          <div class="ml-4">
                            <div class="text-sm font-medium text-gray-900"><%= targetUser.username %></div>
                            <div class="text-sm" style="color: rgb(107 114 128)">ID: #<%= targetUser.id %></div>
                          </div>
                        </div>
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap">
                        <span class="inline-flex px-2 py-1 text-xs font-semibold rounded-full 
                          <%= targetUser.role === 'admin' ? 'bg-red-100 text-red-800' : 
                              targetUser.role === 'moderator' ? 'bg-yellow-100 text-yellow-800' : 
                              'bg-green-100 text-green-800' %>">
                          <%= targetUser.role.charAt(0).toUpperCase() + targetUser.role.slice(1) %>
                        </span>
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-900">
                        <%= targetUser.points || 0 %> pts
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap text-sm" style="color: rgb(107 114 128)">
                        <%= targetUser.created_at ? new Date(targetUser.created_at).toLocaleDateString() : 'N/A' %>
                      </td>
                      <td class="px-6 py-4 whitespace-nowrap text-sm font-medium space-x-2">
                        <a href="/dashboard/users/<%= targetUser.id %>" class="text-blue-600 hover:text-blue-900 transition-colors">View</a>
                        <% if (user.role === 'admin' || (user.role === 'moderator' && targetUser.role !== 'admin')) { %>
                        <a href="/dashboard/users/<%= targetUser.id %>/edit" class="text-indigo-600 hover:text-indigo-900 transition-colors">Edit</a>
                        <% } %>
                        <% if (user.role === 'admin' && targetUser.role !== 'admin') { %>
                        <button onclick="deleteUser(<%= targetUser.id %>)" class="text-red-600 hover:text-red-900 transition-colors">Delete</button>
                        <% } %>
                      </td>
                    </tr>
                    <% }); %>
                  </tbody>
                </table>
                
                <% if (users.length === 0) { %>
                <div class="text-center py-12">
                  <svg class="w-16 h-16 text-gray-300 mx-auto mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                    <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4.354a4 4 0 110 5.292M15 21H3v-1a6 6 0 0112 0v1zm0 0h6v-1a6 6 0 00-9-5.197m13.5-9a2.25 2.25 0 11-4.5 0 2.25 2.25 0 014.5 0z"></path>
                  </svg>
                  <p class="text-gray-500 text-lg">No users found</p>
                  <p class="text-gray-400 text-sm mt-2">Get started by creating your first user</p>
                  <a href="/users/create" class="inline-flex items-center px-4 py-2 mt-4 gradient-bg text-white rounded-lg hover:opacity-90 transition-opacity">
                    <svg class="w-4 h-4 mr-2" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 6v6m0 0v6m0-6h6m-6 0H6"></path>
                    </svg>
                    Add First User
                  </a>
                </div>
                <% } %>
              </div>

              <!-- Pagination -->
              <% if (users.length > 0) { %>
              <div class="px-6 py-3 border-t bg-gray-50" style="border-color: rgb(229 231 235)">
                <div class="flex items-center justify-between">
                  <p class="text-sm text-gray-700">
                    Showing <span class="font-medium">1</span> to <span class="font-medium"><%= users.length %></span> of <span class="font-medium"><%= users.length %></span> users
                  </p>
                  <div class="flex space-x-1">
                    <!-- Pagination controls would go here -->
                  </div>
                </div>
              </div>
              <% } %>
            </div>
        </div>
    </div>

    <script>
        function updateCurrentDate() {
            const now = new Date();
            document.getElementById('currentDate').textContent = now.toLocaleDateString('en-US', {
                weekday: 'short',
                year: 'numeric',
                month: 'short',
                day: 'numeric'
            });
        }
        updateCurrentDate();

        function deleteUser(userId) {
            if (confirm('Are you sure you want to delete this user? This action cannot be undone.')) {
              fetch(`/users/${userId}`, {
                method: 'DELETE',
                headers: {
                  'Content-Type': 'application/json',
                },
              })
              .then(response => {
                if (response.ok) {
                  location.reload();
                } else {
                  alert('Failed to delete user');
                }
              })
              .catch(error => {
                console.error('Error:', error);
                alert('Failed to delete user');
              });
            }
        }

        // Search functionality
        document.querySelector('input[placeholder="Search users..."]').addEventListener('input', function(e) {
            const searchTerm = e.target.value.toLowerCase();
            const rows = document.querySelectorAll('tbody tr');
            
            rows.forEach(row => {
              const text = row.textContent.toLowerCase();
              if (text.includes(searchTerm)) {
                row.style.display = '';
              } else {
                row.style.display = 'none';
              }
            });
        });

        // Role filter
        document.querySelector('select').addEventListener('change', function(e) {
            const filterValue = e.target.value;
            const rows = document.querySelectorAll('tbody tr');
            
            rows.forEach(row => {
              if (filterValue === 'all') {
                row.style.display = '';
              } else {
                const roleCell = row.querySelector('td:nth-child(2) span');
                const role = roleCell.textContent.toLowerCase();
                
                if (role.includes(filterValue)) {
                  row.style.display = '';
                } else {
                  row.style.display = 'none';
                }
              }
            });
        });
    </script>
</body>
</html>