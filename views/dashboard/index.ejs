<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>SHA - Admin Dashboard</title>
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css">
    <script src="https://cdn.jsdelivr.net/npm/chart.js"></script>
    <style>
        :root {
            --color-primary: 59 130 246;
            --color-secondary: 99 102 241;
            --color-accent: 34 197 94;
            --color-background: 249 250 251;
            --color-surface: 255 255 255;
        }
        
        * {
            font-family: "Inter", sans-serif;
        }
        
        .gradient-bg {
            background: linear-gradient(135deg, rgb(var(--color-primary)) 0%, rgb(var(--color-secondary)) 100%);
        }
        
        .card-hover {
            transition: all 0.3s ease;
        }
        .card-hover:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 25px rgba(0,0,0,0.15);
        }
        
        .animate-fade-in {
            animation: fadeIn 0.6s ease-out;
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(20px); }
            to { opacity: 1; transform: translateY(0); }
        }
        
        .stat-card {
            background: linear-gradient(135deg, rgb(var(--color-surface)) 0%, rgb(248 250 252) 100%);
            border: 1px solid rgb(226 232 240);
        }
        
        .nav-item {
            transition: all 0.2s ease;
        }
        .nav-item:hover {
            background-color: rgb(241 245 249);
            border-left: 4px solid rgb(var(--color-primary));
        }
        .nav-item.active {
            background-color: rgb(239 246 255);
            border-left: 4px solid rgb(37 99 235);
            color: rgb(29 78 216);
        }
    </style>
</head>
<body class="min-h-screen" style="background-color: rgb(var(--color-background))">
    <!-- Top Navigation -->
    <nav class="shadow-sm border-b fixed w-full top-0 z-50" style="background-color: rgb(var(--color-surface)); border-color: rgb(229 231 235)">
        <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
            <div class="flex justify-between h-16">
                <div class="flex items-center">
                    <img class="h-8 w-auto" src="https://shacpr.org.sa/images/sha.png" alt="SHA Logo">
                    <div class="ml-4">
                        <h1 class="text-xl font-semibold text-gray-900">Admin Dashboard</h1>
                    </div>
                </div>
                
                <div class="flex items-center space-x-4">
                    <div class="flex items-center space-x-2">
                        <div class="w-2 h-2 bg-green-400 rounded-full animate-pulse"></div>
                        <span class="text-sm text-gray-500">Live</span>
                    </div>
                    <div class="text-sm text-gray-600">
                        <i class="fas fa-calendar mr-1"></i>
                        <span id="currentDate"></span>
                    </div>
                    <a href="/" class="text-gray-600 hover:text-gray-900 px-3 py-2 rounded-md text-sm font-medium">
                        <i class="fas fa-home mr-1"></i>Main Site
                    </a>
                    <a href="/auth/logout" class="bg-red-600 hover:bg-red-700 text-white px-4 py-2 rounded-md text-sm font-medium">
                        <i class="fas fa-sign-out-alt mr-1"></i>Logout
                    </a>
                </div>
            </div>
        </div>
    </nav>

    <div class="flex pt-16">
        <!-- Sidebar Navigation -->
        <div class="w-64 shadow-sm border-r min-h-screen" style="background-color: rgb(var(--color-surface)); border-color: rgb(229 231 235)">
            <div class="p-6">
                <div class="flex items-center space-x-3 mb-6">
                    <img class="h-8 w-8" src="https://shacpr.org.sa/images/sha.png" alt="SHA Logo">
                    <h1 class="text-xl font-bold gradient-bg bg-clip-text text-transparent">Admin Dashboard</h1>
                </div>
                
                <nav class="space-y-1">
                    <!-- Main Navigation -->
                    <a href="/dashboard" class="nav-item active flex items-center px-4 py-3 text-sm font-medium rounded-lg">
                        <i class="fas fa-tachometer-alt mr-3 text-lg"></i>
                        Dashboard Overview
                    </a>
                    
                    <!-- Analytics Section -->
                    <div class="pt-4">
                        <div class="px-4 text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">Analytics</div>
                    </div>
                    <a href="/dashboard/transactions" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-credit-card mr-3 text-lg"></i>
                        Transactions
                    </a>

                    <!-- Management Section -->
                    <div class="pt-4">
                        <div class="px-4 text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">Management</div>
                    </div>
                    <a href="/dashboard/certificates" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-certificate mr-3 text-lg"></i>
                        Certificates
                    </a>
                    <a href="/dashboard/users" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-users mr-3 text-lg"></i>
                        User Management
                    </a>
                    <a href="/dashboard/subscriptions" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-crown mr-3 text-lg"></i>
                        Subscriptions
                    </a>
                    <a href="/dashboard/templates" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-file-alt mr-3 text-lg"></i>
                        Certificate Templates
                    </a>

                    <!-- System Section -->
                    <div class="pt-4">
                        <div class="px-4 text-xs font-semibold text-gray-400 uppercase tracking-wider mb-2">System</div>
                    </div>
                    <a href="/dashboard/settings" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-cog mr-3 text-lg"></i>
                        System Settings
                    </a>
                    <a href="/dashboard/email" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-envelope mr-3 text-lg"></i>
                        Bulk Email
                    </a>
                    <a href="/dashboard/audit" class="nav-item flex items-center px-4 py-3 text-sm font-medium rounded-lg text-gray-700">
                        <i class="fas fa-clipboard-list mr-3 text-lg"></i>
                        Audit Logs
                    </a>
                </nav>
            </div>
        </div>

        <!-- Main Content -->
        <div class="flex-1 p-8">
            <!-- Dashboard Header -->
            <div class="mb-8 animate-fade-in">
                <h2 class="text-3xl font-bold text-gray-900 mb-2">Dashboard Overview</h2>
                <p class="text-gray-600">Real-time insights and system performance metrics</p>
            </div>

            <!-- Dashboard Stats -->
            <div class="grid grid-cols-1 md:grid-cols-4 gap-6 mb-8 animate-fade-in">
                <div class="bg-white rounded-xl shadow-sm p-6 card-hover border border-gray-200">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm font-medium text-gray-600">Total Revenue</p>
                            <p class="text-3xl font-bold text-green-600" id="totalRevenue">$<%= overview.revenue.totalLifetime || overview.revenue.total || 0 %></p>
                            <p class="text-xs text-gray-500 mt-1">
                                <span class="<%= (overview.revenue.growth || 0) >= 0 ? 'text-green-500' : 'text-red-500' %>">
                                    <%= (overview.revenue.growth || 0) >= 0 ? '+' : '' %><%= (overview.revenue.growth || 0).toFixed(1) %>%
                                </span> this period
                            </p>
                        </div>
                        <div class="bg-green-100 p-3 rounded-full">
                            <i class="fas fa-dollar-sign text-green-600 text-xl"></i>
                        </div>
                    </div>
                </div>

                <div class="bg-white rounded-xl shadow-sm p-6 card-hover border border-gray-200">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm font-medium text-gray-600">Active Certificates</p>
                            <p class="text-3xl font-bold text-blue-600" id="totalCertificates"><%= overview.certificates.total || 0 %></p>
                            <p class="text-xs text-gray-500 mt-1">
                                <span class="<%= (overview.certificates.growth || 0) >= 0 ? 'text-blue-500' : 'text-red-500' %>">
                                    <%= (overview.certificates.growth || 0) >= 0 ? '+' : '' %><%= overview.certificates.growth || 0 %>
                                </span> new this period
                            </p>
                        </div>
                        <div class="bg-blue-100 p-3 rounded-full">
                            <i class="fas fa-certificate text-blue-600 text-xl"></i>
                        </div>
                    </div>
                </div>

                <div class="bg-white rounded-xl shadow-sm p-6 card-hover border border-gray-200">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm font-medium text-gray-600">Total Users</p>
                            <p class="text-3xl font-bold text-purple-600" id="totalUsers"><%= overview.users.total || 0 %></p>
                            <p class="text-xs text-gray-500 mt-1">
                                <span class="text-purple-500">+<%= overview.users.growth || 0 %></span> today
                            </p>
                        </div>
                        <div class="bg-purple-100 p-3 rounded-full">
                            <i class="fas fa-users text-purple-600 text-xl"></i>
                        </div>
                    </div>
                </div>

                <div class="bg-white rounded-xl shadow-sm p-6 card-hover border border-gray-200">
                    <div class="flex items-center justify-between">
                        <div>
                            <p class="text-sm font-medium text-gray-600">System Status</p>
                            <p class="text-3xl font-bold text-green-600">Online</p>
                            <p class="text-xs text-gray-500 mt-1">
                                <span class="text-green-500">99.9%</span> uptime
                            </p>
                        </div>
                        <div class="bg-green-100 p-3 rounded-full">
                            <i class="fas fa-server text-green-600 text-xl"></i>
                        </div>
                    </div>
                </div>
            </div>

            <!-- Charts Section -->
            <div class="grid grid-cols-1 lg:grid-cols-2 gap-8 mb-8">
                <!-- Revenue Chart -->
                <div class="bg-white rounded-xl shadow-sm p-6 card-hover border border-gray-200">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-lg font-semibold text-gray-900">Revenue Trend</h3>
                        <div class="flex items-center space-x-2">
                            <div class="w-2 h-2 bg-blue-500 rounded-full"></div>
                            <span class="text-sm text-gray-500">Last 30 days</span>
                        </div>
                    </div>
                    <div class="relative" style="height: 300px; max-height: 400px;">
                        <canvas id="revenueChart"></canvas>
                    </div>
                </div>

                <!-- Certificates Chart -->
                <div class="bg-white rounded-xl shadow-sm p-6 card-hover border border-gray-200">
                    <div class="flex items-center justify-between mb-4">
                        <h3 class="text-lg font-semibold text-gray-900">Certificate Generation</h3>
                        <div class="flex items-center space-x-2">
                            <div class="w-2 h-2 bg-green-500 rounded-full"></div>
                            <span class="text-sm text-gray-500">Last 30 days</span>
                        </div>
                    </div>
                    <div class="relative" style="height: 300px; max-height: 400px;">
                        <canvas id="certificatesChart"></canvas>
                    </div>
                </div>
            </div>

            <!-- Recent Activity -->
            <div class="bg-white rounded-xl shadow-sm border border-gray-200 card-hover">
                <div class="px-6 py-4 border-b border-gray-200">
                    <h3 class="text-lg font-semibold text-gray-900">Recent Activity</h3>
                </div>
                <div class="p-6">
                    <div class="space-y-4" id="recentActivity">
                        <% if (recentActivity && recentActivity.length > 0) { %>
                            <% recentActivity.forEach(activity => { %>
                            <div class="flex items-center space-x-4 p-3 rounded-lg hover:bg-gray-50">
                                <div class="flex-shrink-0">
                                    <% if (activity.type === 'certificate') { %>
                                        <div class="w-8 h-8 bg-green-100 rounded-full flex items-center justify-center">
                                            <i class="fas fa-certificate text-green-600 text-sm"></i>
                                        </div>
                                    <% } else if (activity.type === 'user') { %>
                                        <div class="w-8 h-8 bg-blue-100 rounded-full flex items-center justify-center">
                                            <i class="fas fa-user text-blue-600 text-sm"></i>
                                        </div>
                                    <% } else if (activity.type === 'payment') { %>
                                        <div class="w-8 h-8 bg-purple-100 rounded-full flex items-center justify-center">
                                            <i class="fas fa-credit-card text-purple-600 text-sm"></i>
                                        </div>
                                    <% } %>
                                </div>
                                <div class="flex-1">
                                    <p class="text-sm font-medium text-gray-900"><%= activity.description %></p>
                                    <p class="text-xs text-gray-500">
                                        <%= new Date(activity.timestamp).toLocaleString() %>
                                    </p>
                                </div>
                            </div>
                            <% }); %>
                        <% } else { %>
                            <p class="text-gray-500 text-center py-4">No recent activity</p>
                        <% } %>
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script>
        // Update current date
        document.addEventListener('DOMContentLoaded', function() {
            const now = new Date();
            const dateString = now.toLocaleDateString('en-US', {
                weekday: 'long',
                year: 'numeric', 
                month: 'long',
                day: 'numeric'
            });
            const dateElement = document.getElementById('currentDate');
            if (dateElement) {
                dateElement.textContent = dateString;
            }

            // Initialize charts if data is available
            <% if (revenueChart && certificatesChart) { %>
            initializeCharts();
            <% } %>
        });

        <% if (revenueChart && certificatesChart) { %>
        function initializeCharts() {
            // Revenue Chart
            const revenueCtx = document.getElementById('revenueChart');
            if (revenueCtx) {
                new Chart(revenueCtx, {
                    type: 'line',
                    data: {
                        labels: <%- JSON.stringify(revenueChart.labels) %>,
                        datasets: [{
                            label: 'Revenue ($)',
                            data: <%- JSON.stringify(revenueChart.data) %>,
                            borderColor: 'rgb(59, 130, 246)',
                            backgroundColor: 'rgba(59, 130, 246, 0.1)',
                            fill: true,
                            tension: 0.4
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: {
                                display: false
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true,
                                ticks: {
                                    callback: function(value) {
                                        return '$' + value.toLocaleString();
                                    }
                                }
                            }
                        }
                    }
                });
            }

            // Certificates Chart
            const certsCtx = document.getElementById('certificatesChart');
            if (certsCtx) {
                new Chart(certsCtx, {
                    type: 'bar',
                    data: {
                        labels: <%- JSON.stringify(certificatesChart.labels) %>,
                        datasets: [{
                            label: 'Certificates',
                            data: <%- JSON.stringify(certificatesChart.data) %>,
                            backgroundColor: 'rgba(34, 197, 94, 0.8)',
                            borderColor: 'rgb(34, 197, 94)',
                            borderWidth: 1
                        }]
                    },
                    options: {
                        responsive: true,
                        maintainAspectRatio: false,
                        plugins: {
                            legend: {
                                display: false
                            }
                        },
                        scales: {
                            y: {
                                beginAtZero: true
                            }
                        }
                    }
                });
            }
        }
        <% } %>
    </script>
</body>
</html>